program moresophisticated;

        type
                r1 = array [1..10] of integer;
  var
      i,j,s: integer;
      b: boolean;
        c : boolean;
        arr : r1;

  function f(i:integer):integer;

    var a: integer;

    begin
        arr[i] := arr[j];
        b := i > 1;
        c := i < 1;
      if ((a < 2) and (a > 1)) then f:=1
    end;

  begin
    end.
